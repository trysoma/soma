issuer: http://localhost:5556/dex
storage:
  type: memory

web:
  http: 0.0.0.0:5556

oauth2:
  skipApprovalScreen: true

staticClients:
  - id: trysoma.ai
    redirectURIs:
      - http://127.0.0.1:3000/api/identity/v1/auth/callback/mock
      - http://localhost:3000/api/identity/v1/auth/callback/mock
      - http://localhost:8080/callback
    name: Example App
    secret: example-secret

connectors:
  - type: mockCallback
    id: mock
    name: Mock