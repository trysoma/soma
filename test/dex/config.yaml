issuer: http://localhost:5556/dex
storage:
  type: memory

web:
  http: 0.0.0.0:5556

staticClients:
  - id: trysoma.ai
    redirectURIs:
      - http://localhost:3000/api/v1/auth/callback
    name: Example App
    secret: example-secret

connectors:
  - type: mockCallback
    id: mock
    name: Mock