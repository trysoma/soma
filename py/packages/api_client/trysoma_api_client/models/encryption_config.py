# coding: utf-8

"""
soma

No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

The version of the OpenAPI document: v1
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from trysoma_api_client.models.envelope_key_config import EnvelopeKeyConfig
from typing import Optional, Set
from typing_extensions import Self


class EncryptionConfig(BaseModel):
    """
    Top-level encryption configuration
    """  # noqa: E501

    envelope_keys: Optional[Dict[str, EnvelopeKeyConfig]] = Field(
        default=None,
        description='Map of envelope key id (ARN or file_name) -> envelope key configuration with nested DEKs DEKs are stored by their alias name (e.g., "default") rather than UUID',
    )
    __properties: ClassVar[List[str]] = ["envelope_keys"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EncryptionConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in envelope_keys (dict)
        _field_dict = {}
        if self.envelope_keys:
            for _key_envelope_keys in self.envelope_keys:
                if self.envelope_keys[_key_envelope_keys]:
                    _field_dict[_key_envelope_keys] = self.envelope_keys[
                        _key_envelope_keys
                    ].to_dict()
            _dict["envelope_keys"] = _field_dict
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EncryptionConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "envelope_keys": dict(
                    (_k, EnvelopeKeyConfig.from_dict(_v))
                    for _k, _v in obj["envelope_keys"].items()
                )
                if obj.get("envelope_keys") is not None
                else None
            }
        )
        return _obj
