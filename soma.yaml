project: my-project
agent: my-agent
description: My Soma Agent
name: My Agent
version: 1.0.0

# Bridge configuration (added after running bridge init)
# bridge:
#   encryption:
#     example_data_encryption_key_id: "some-key-id"
