## v0.0.2 - 2025-11-27
### Added (Patch, new features)
* Added bash completions, project init command to soma CLI
* Added secrets management to core platform. you can set secrets using the CLI "soma secret set" and "soma secret rm". "soma secret list" will list all 
### Patch (Other, non-breaking changes)
* trigger a release, fix napi build process
* Moved encryption management. shifted encryption management into it's own crate for re-usability outside of just the bridge MCP server.
